<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_registration_list" name="Registrations">
        <t t-call="portal.portal_layout">
            <h1>Your Registrations</h1>
            <br/>
            <t t-if="registration_closed">
                <div class="alert alert-warning">
                    The registration period is over.
                </div>
            </t>
            <t t-else="">
                <div class="mt-2 mb-2">
                    <button type="button" id="btn-create-registration" class="btn btn-primary btn-lg"
                            onclick="window.location='/my/registrations/create'">
                        <i class="fa fa-plus"/>
                        <span>New</span>
                    </button>
                </div>
            </t>

            <br/>
            <t t-if="registrations" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Name</th>
                        <th style="text-align: center">Seminar?</th>
                        <th>Grade</th>
                        <th>Room Type</th>
                        <th style="text-align: center">Vegan/Vegetarian</th>
                        <th style="text-align: center">Allergies</th>
                        <th style="text-align: center">Parking</th>
                        <th style="text-align: center">Shuttle</th>

                    </tr>
                </thead>
                <t t-foreach="registrations" t-as="reg">
                    <tr>
                        <!-- Name -->
                        <td>
                            <span t-field="reg.name"/>
                        </td>
                        <!-- Seminar -->
                        <td style="text-align: center">
                            <t t-if="reg.participates_in_seminar">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Grade -->
                        <td>
                            <span t-field="reg.grade_description"/>
                        </td>
                        <!-- Room Type -->
                        <td>
                            <span t-field="reg.room_preference"/>
                        </td>
                        <!-- Vegi -->
                        <td style="text-align: center">

                            <t t-if="reg.is_vegetarian or reg.is_vegan">
                                <i class="fa fa-check"/>
                            </t>

                        </td>
                        <!-- Allergies -->
                        <td style="text-align: center">
                            <t t-if="reg.has_allergies">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Parking -->
                        <td style="text-align: center">
                            <t t-if="reg.needs_parking_lot">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Shuttle -->
                        <td style="text-align: center">
                            <t t-if="reg.needs_shuttle">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                    </tr>
                </t>
            </t>
            <t t-else="">
                <p>You have not registered any participants yet.</p>
            </t>
        </t>
    </template>


</odoo>