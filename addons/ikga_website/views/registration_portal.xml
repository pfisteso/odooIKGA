<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_registration_list" name="Registrations">
        <t t-call="portal.portal_layout">
            <h1>Your Registrations</h1>
            <br/>
            <div class="mt-2 mb-2">
                <button type="button" id="btn-create-registration" class="btn btn-primary btn-lg"
                        onclick="window.location='/my/registrations/create'">
                    <i class="fa fa-plus"/>
                    <span>New</span>
                </button>
            </div>

            <br/>
            <t t-if="registrations" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Name</th>
                        <th>Birthdate</th>
                        <th>Seminar?</th>
                        <th>Grade</th>
                        <th>Room Type</th>
                        <th>Vegan/Vegetarian/Allergies</th>
                        <th>Parking</th>
                        <th>Shuttle</th>
                        <th>View/Edit</th>
                    </tr>
                </thead>
                <t t-foreach="registrations" t-as="reg">
                    <tr>
                        <!-- Name -->
                        <td>
                            <span t-field="reg.name"/>
                        </td>
                        <!-- Birthdate -->
                        <td>
                            <span t-field="reg.birthdate"/>
                        </td>
                        <!-- Seminar -->
                        <td>
                            <t t-if="reg.participates_in_seminar">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Grade -->
                        <td>
                            <span t-field="reg.grade_description"/>
                        </td>
                        <!-- Room Type -->
                        <td>
                            <span t-field="reg.room_preference"/>
                        </td>
                        <!-- VVA -->
                        <td>
                            <t t-if="reg.is_vegetarian or reg.is_vegan or reg.has_allergies">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Parking -->
                        <td>
                            <t t-if="reg.need_parking_lot">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Shuttle -->
                        <td>
                            <t t-if="reg.interested_in_shuttle_service">
                                <i class="fa fa-check"/>
                            </t>
                        </td>
                        <!-- Edit -->
                        <td>
                            <a t-att-href="'/my/registrations/edit/%s' % reg.id">
                                <button type="button" class="btn btn-secondary">
                                    <i class="fa fa-pencil"/>
                                </button>
                            </a>
                        </td>
                    </tr>
                </t>
            </t>
            <t t-else="">
                <p>You have not registered any participants yet.</p>
            </t>
        </t>
    </template>



</odoo>